/**
 * User Service Types
 * User profile and authentication service types
 */

import type { User } from '../../common';

// Use the centralized User type as UserProfile
export type UserProfile = User;